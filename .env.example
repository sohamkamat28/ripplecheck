# fixture runs with no credentials and is the default
RIPPLECHECK_MODE=fixture

# live mode starts the official DataHub MCP server over stdio
# RIPPLECHECK_MODE=live
# DATAHUB_MCP_COMMAND=npx -y @acryldata/mcp-server-datahub
# TOOLS_IS_MUTATION_ENABLED=true

